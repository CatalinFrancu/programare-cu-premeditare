\ProvidesPackage{preamble}

\usepackage[romanian]{babel}
\usepackage{emoji}
% We don't really need it, but it suppresses some warnings from hyperref.
\usepackage{float}
\usepackage{fontspec} % for the Inconsolata font
\usepackage[a4paper, margin=1.8cm]{geometry}
\usepackage{parskip}
\usepackage[onehalfspacing]{setspace}
\usepackage[svgnames]{xcolor}

\usepackage{biblatex}
\addbibresource{references.bib}

% for enumerations with \alph, \Alph etc.; for better control of spacing
% within in around lists (itemize, enumerate)
\usepackage{enumitem}
\setlist{noitemsep}

\usepackage[pdfpagelabels]{hyperref}
\hypersetup{
  colorlinks=true,
  linkcolor=blue,
  filecolor=blue,
  urlcolor=blue,
}

% A more legible font for source code.
\usepackage{inconsolata-nerd-font}
\setmonofont[
Scale=0.85, % Inconsolata looks naturally larger than the serif font.
BoldFont={InconsolataLGCNerdFont-Bold.ttf},
ItalicFont={InconsolataLGCNerdFont-Italic.ttf},
BoldItalicFont={InconsolataLGCNerdFont-BoldItalic.ttf}
]{InconsolataLGCNerdFont-Regular.ttf}

\usepackage{minted}
\usemintedstyle{trac}
\setminted{
  bgcolor=black!02,
  fontsize=\footnotesize,
}

% Recommended by babel + biblatex. Must be loaded after Minted.
\usepackage{csquotes}

\usepackage{tikz}
\usetikzlibrary{positioning}

\def\bigoh{\mathcal{O}}
\newcommand{\ccode}[1]{\mintinline{c}{#1}}
